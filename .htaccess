<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Do not rewrite if the request already targets the public directory
    RewriteCond %{REQUEST_URI} !^/public/
    
    # Transparently route all traffic to the public/ folder
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
